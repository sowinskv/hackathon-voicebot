# Fast development mode - skips health checks for instant start
services:
  postgres:
    healthcheck:
      interval: 1s
      timeout: 1s
      retries: 2
      start_period: 2s

  api-gateway:
    depends_on:
      postgres:
        condition: service_started  # Don't wait for healthy
